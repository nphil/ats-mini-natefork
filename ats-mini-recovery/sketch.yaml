profiles:
  esp32s3-ospi:
    # If you change this line, change it in build.yml as well
    # FlashMode=dio is REQUIRED on octal-PSRAM (PSRAM=opi) boards — see ats-mini/sketch.yaml.
    fqbn: esp32:esp32:esp32s3:CDCOnBoot=cdc,FlashSize=8M,PSRAM=opi,CPUFreq=80,USBMode=hwcdc,FlashMode=dio,PartitionScheme=custom,DebugLevel=none
    platforms:
      - platform: esp32:esp32 (3.3.10)
        platform_index_url: https://espressif.github.io/arduino-esp32/package_esp32_index.json
    libraries:
      - TFT_eSPI (2.5.43)

default_profile: esp32s3-ospi
